# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/

# Virtual environment
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Credentials / local env — never commit (see .pre-commit-config.yaml for gitleaks)
.env
.env.*
!.env.example

# Project specific
# Ignore results CONTENTS (not the dir) so the sample-result exceptions below work.
results/*
secrets/
*.json
!p4/**/*.json
# Keep the bundled sample results so the analysis notebook runs without a slice
!results/fabric_alice_results.json
!results/fabric_bob_results.json
!results/fabric_seq_alice.json
!results/fabric_seq_bob.json
!results/fabric_e91_alice.json
!results/fabric_e91_bob.json
!qne-sequence/results/sequence_scenarios.json
!results/decoy_scenarios.json
*.pcap

# Personal session tracking — never ship in the artifact
cc-usage-log.md

# Paper drafts + generated figures — kept local, regenerated via paper/make_figures.py
paper/

# Internal review notes — keep local, don't ship
REVIEW_*.md

# P4 build artifacts
p4/bmv2/*.json
p4/bmv2/**/*.json
p4/tna/build/

# Jupyter
.ipynb_checkpoints/

# Tool caches
.pytest_cache/
.ruff_cache/

# BMv2 logs
*.log

# Vagrant
.vagrant/
